# --- Required for AI tailoring ---
# Get one at https://console.anthropic.com  → API Keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# Optional: override the model (defaults to claude-sonnet-5)
# ANTHROPIC_MODEL=claude-sonnet-5

# Optional bounded role research. Enabled by default; set to false to tailor
# from the complete job description only.
# ROLE_RESEARCH_ENABLED=true

# Cache lifetime for normalized role + functional-domain research (30-60 days).
# Default: 45 days.
# ROLE_RESEARCH_CACHE_DAYS=45

# --- Live jobs ---
# Required for real general listings unless a durable snapshot already exists.
# Use DEMO_MODE=true explicitly when you want labelled sample data locally.
# Get a key at https://hirebase.org/
HIREBASE_API_KEY=your-hirebase-api-key

# Optional Hirebase breadth controls. Defaults shown below; the server clamps
# unsafe values. The sync budget limits provider records requested per refresh.
# HIREBASE_MAX_RESULTS=250
# HIREBASE_SYNC_JOB_BUDGET=250
# HIREBASE_RESULTS_PER_PAGE=20
# HIREBASE_MAX_PAGES_PER_GROUP=2
# HIREBASE_TITLES_PER_QUERY=50
# Minimum interval between Hirebase provider attempts (5-60 minutes).
# HIREBASE_REFRESH_COOLDOWN_MINUTES=15

# Used only by the "LinkedIn only" toggle. Keep this server-side.
# Get a key at https://theirstack.com/
THEIRSTACK_API_KEY=your-theirstack-api-key

# Optional LinkedIn refresh cap (25-250). Default: 150 returned records.
# THEIRSTACK_MAX_RESULTS=150

# Private durable storage for the profile, job feeds, and restart-safe request
# guards. Required for TheirStack and recommended for production Hirebase;
# without it, local Hirebase development falls back to process memory only.
# Use the same token on every server instance.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxxxxxx

# Optional: disable real Claude, Hirebase, and TheirStack calls.
# DEMO_MODE=true

# --- Site password gate ---
# Required to protect the paid API usage behind this site from public/bot traffic.
SITE_PASSWORD=change-me
